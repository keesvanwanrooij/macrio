# Plan: v0.2.0 — auth emails + forgot password

| Field | Value |
|---|---|
| Status | Done |
| Roadmap | [`ROADMAP_MINOR.md`](../ROADMAP_MINOR.md) → **v0.2.0** |
| Depends on | — |
| Related | `SETUP.md`, `backend/supabase/email-templates/` |

## Problem

Confirm email is off for testing; default Supabase emails are unbranded; no forgot-password path.

## Goal

Production-ready auth emails (confirm + reset) with Macrio branding, and an in-app forgot-password flow.

## Acceptance criteria

- [x] New user must confirm email before session (when confirm is ON) - already handled in sign-up; SETUP documents toggle
- [x] Forgot password sends reset mail and completes reset in app
- [x] Copy in `en.json` / `nl.json`
- [x] `SETUP.md` documents founder-testing vs production + redirect URLs + templates

## Shipped notes

- Screens: `(auth)/forgot-password`, `(auth)/reset-password`, `auth/callback`
- PKCE + `Linking.createURL('/auth/callback')` for reset redirect
- Templates: `backend/supabase/email-templates/` (paste into Supabase dashboard)
