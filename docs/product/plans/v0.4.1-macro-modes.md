# Plan: v0.4.1 — goal macro modes (Simple / Athlete / Keto)

| Field | Value |
|---|---|
| Status | **Superseded** (unified % editor; modes removed from UI) |
| Roadmap | [`ROADMAP_MINOR.md`](../ROADMAP_MINOR.md) → Group B → v0.4.1 |
| Depends on | v0.4.0 GoalMacroEditor |
| Related | onboarding, Settings → goals |

> **Status (2026-07-23):** Mode chips (Simple / Athlete / Keto) and mode-specific math were replaced by one percent-based editor (default 50/20/30). Column `goal_macro_mode` removed in migration `021_drop_goal_macro_mode.sql`. Protein g/kg calculator defaults → [`docs/context/protein-g-per-kg.md`](../../context/protein-g-per-kg.md).

## Problem

One g/kg cascade for everyone is hard to understand. Beginners think in %, lifters think in g/kg protein, keto needs high fat %.

## Goal

Pick a **macro style**; sliders and cascade match that style. Switching style resets macros to that style’s defaults from current kcal.

## Locked decisions

| Topic | Choice |
|---|---|
| Modes | `simple` (EN Simple / NL Simpel), `athlete` (Athlete / Sporter), `keto` (Keto) |
| Selector label | EN Macro style / NL Macrostijl |
| Simple defaults | C 50 / P 20 / F 30 (% of kcal) |
| Keto defaults | C 5 / P 25 / F 70 |
| % slider scale | `max(60, highest current %)` for all % bars (simple, keto, athlete fat) |
| % cascade | Ring: protein→fat, fat→carbs, carbs→protein; **kcal fixed** |
| Athlete protein | Slider **0.8–2.0** g/kg; defaults lose **1.5** / maintain **1.3** / gain **1.4** |
| Athlete fat | % of kcal (default **30%**); scale as above |
| Athlete carbs | No slider; fills remaining. Editing carbs grams adjusts **fat**; protein stays on g/kg |
| Mode switch | Reset to mode defaults from current kcal (+ weight for athlete) |
| Persist | `profiles.goal_macro_mode` |
| Version | **0.4.1** patch |

## Out of scope

- Custom / free-grams mode
- Auto-save vs Save unification (separate polish)
- Create-product % sliders (v0.13; reuse helpers later)

## Acceptance criteria

- [x] Mode chips/selector on onboarding + Settings
- [x] Simple / Keto / Athlete behaviours as locked
- [x] Dynamic % scale `max(60, highest %)`
- [x] Mode switch resets defaults
- [x] nl/en i18n; migration for `goal_macro_mode`
- [x] Version 0.4.1 in VERSION / package / app.config / CHANGELOG

## Implementation notes

- Shared pure math in `goalCalculator.ts` (percent ring, athlete cascade, `goalsFromMode`)
- `GoalMacroEditor` driven by mode; drop carbs remount hack where possible
- Migration `019_goal_macro_mode.sql`

## Test plan

- [ ] Simple: drag protein → fat moves; kcal unchanged; scale stays 60 until a % exceeds 60
- [ ] Keto: fat default 70; scale ≥ 70; ring still sums ~100%
- [ ] Athlete: protein g/kg; fat %; carbs fill; edit carbs → fat moves; protein g/kg stable
- [ ] Switch Simple→Keto resets to keto defaults from same kcal
- [ ] Save persists mode + goals
